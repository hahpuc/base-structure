export enum EStatus {
  inactive,
  active,
}

export enum ESystemType {
  text,
  number,
  textarea,
  password,
  json,
}

export enum EUploadType {
  image = 'image/png, image/jpeg',
}

export enum ELocale {
  vi = 'vi',
  en = 'en',
}
